#include <stdio.h>
 /**
  * main - printing code using printf
  * Return:0 (cuccess)
  */
int main (void)
{
       printf("with proper grammar, but the outcome is a piece of art,\n");
       return (0);
}
